# Environment Variables Template
# Copy this file to .env.local and update with your actual values

# Frontend environment variables
# This should point to your backend API server, not the frontend domain

# For local development (if backend runs on different port):
# NEXT_PUBLIC_API_URL=http://localhost:8080

# For production deployment on Render:
# Set this in your Render dashboard environment variables
# NEXT_PUBLIC_API_URL=https://your-backend-render-url.onrender.com

# Default (makes requests to frontend instead of backend - INCORRECT):
NEXT_PUBLIC_API_URL=/api